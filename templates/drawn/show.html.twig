{% extends 'base.html.twig' %}

{% block title %}Drawn{% endblock %}

{% block body %}
    <h1>Drawn</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ drawn.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ drawn.name }}</td>
            </tr>
            <tr>
                <th>Crew_id</th>
                <td>{{ drawn.crewId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('drawn_index') }}">back to list</a>

    <a href="{{ path('drawn_edit', {'id': drawn.id}) }}">edit</a>

    {{ include('drawn/_delete_form.html.twig') }}
{% endblock %}
